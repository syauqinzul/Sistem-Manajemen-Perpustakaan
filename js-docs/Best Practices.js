// 1. Use const by default
const DEFAULT_CONFIG = { theme: 'dark' };

// 2. Use let only when reassignment needed
let counter = 0;
counter = counter + 1;

// 3. Avoid var
// 4. Descriptive names
const MAX_BOOKS = 100;          // CONSTANTS_UPPERCASE
let currentPage = 1;            // camelCase variables