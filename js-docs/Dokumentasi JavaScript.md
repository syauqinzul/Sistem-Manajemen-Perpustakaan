
---

## 🎯 COMMIT 4: Dokumentasi JavaScript

### File: `js-docs/01-variables-data-types.md`
```markdown
# 🧮 VARIABLES & DATA TYPES

## File: `script.js`

### 1. Variable Declaration
```javascript
// VAR (ES5 - Avoid)
var oldWay = "deprecated";  // Function-scoped

// LET (ES6+ - Mutable)
let counter = 0;            // Block-scoped, can reassign

// CONST (ES6+ - Immutable reference)
const PI = 3.14;            // Block-scoped, cannot reassign
const books = [];           // Array reference is constant