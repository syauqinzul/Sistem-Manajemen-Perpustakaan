# 📄 STRUKTUR HTML DASAR

## File: `index.html`

### 1. DOCTYPE Declaration
```html
<!DOCTYPE html>

Deklarasi versi HTML (HTML5)

Browser parsing mode standar

Tanpa ini → "quirks mode" (rendering tidak konsisten)